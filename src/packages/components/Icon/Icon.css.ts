import { style } from '@vanilla-extract/css';

export const iconContainer = style({
    width: '16px',
    height: '16px',
    display: 'inline-flex',
    overflow: 'hidden',
    fill: 'currentColor',
    verticalAlign: 'top',
});
