import { style } from '@vanilla-extract/css';

export const iconContainer = style({
    display: 'inline-flex',
    width: '16px',
    height: '16px',
    fill: 'currentColor',
    overflow: 'hidden',
    verticalAlign: 'top',
});
